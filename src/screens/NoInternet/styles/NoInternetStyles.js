import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    fontWeight: 600,
  },
  checkConnectionButtonContainer: {
    marginTop: 10,
  },
  checkConnectionText: {
    color: '#0e7afe',
  },
});

export default styles;
